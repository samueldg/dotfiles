mkcd () {
    mkdir "$@" && cd "$_";
}

sublp () {
    PROJECT=$(find "$@" -maxdepth 1 -type f -name '*.sublime-project')
    if [ -n "$PROJECT" ]; then
        subl $PROJECT
    else
        subl "$@"
    fi
}
